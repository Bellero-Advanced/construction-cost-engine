% figures/chart-outlier.tex — KV coverage snapshot (real data)
% Source: GET /api/sources/health (snapshot: 2026-05-24)
\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      width=12cm, height=6cm,
      ybar,
      bar width=10pt,
      xlabel={แหล่งข้อมูล},
      ylabel={จำนวน records ใน KV},
      symbolic x coords={TPSO,CGD,DIT,HomePro,Boonthavorn,MegaHome,GlobalHouse,ThaiWatsadu,BnB,SCGHome,Dohome},
      xtick=data,
      x tick label style={rotate=35,anchor=east,font=\scriptsize},
      ymin=0, ymax=30,
      nodes near coords,
      nodes near coords align={vertical},
      every node near coord/.append style={font=\scriptsize},
      grid=both, grid style={dashed,gray!25},
      tick label style={font=\footnotesize},
      label style={font=\footnotesize},
    ]
      \addplot[fill=blue!40,draw=blue!70!black] coordinates {
        (TPSO,1) (CGD,24) (DIT,9) (HomePro,4) (Boonthavorn,11)
        (MegaHome,0) (GlobalHouse,0) (ThaiWatsadu,0) (BnB,0) (SCGHome,0) (Dohome,0)
      };
    \end{axis}
  \end{tikzpicture}
  \caption{จำนวน records ที่มีสถานะ \texttt{fresh} ใน Cloudflare KV ณ วันที่ 24 พ.ค.\ 2569 (ดึงจาก \texttt{/api/sources/health}). จากทั้งหมด 297 cells (11 แหล่ง $\times$ 27 วัสดุ) มี 49 cells ที่มีข้อมูล (coverage 16.5\%). แหล่งที่ยังไม่มีข้อมูล (MegaHome, Global House, Thai Watsadu, BnB, SCG Home, Dohome) เป็นเว็บไซต์ SPA ที่ต้องใช้ AI agent research ซึ่งอยู่ระหว่างการพัฒนา. ข้อมูลนี้แสดงสถานะของระบบในช่วงทดลองใช้งาน ไม่ใช่ขีดจำกัดของสถาปัตยกรรม.}
  \label{fig:chart-outlier}
\end{figure}
