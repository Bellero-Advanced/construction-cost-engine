% figures/architecture-diagram.tex — สถาปัตยกรรมระบบ (TikZ, fixed layout)
\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    font=\footnotesize,
    every node/.style={align=center},
    layer/.style={draw,rounded corners=3pt,minimum height=1.0cm,
                  text width=13cm,fill=gray!5,inner sep=6pt},
    lbl/.style={font=\bfseries\scriptsize,anchor=west},
    arr/.style={-{Stealth[length=2.5mm]},thick,gray!60},
  ]
    % Layer 1
    \node[layer,fill=orange!10] (L1) at (0,0)
      {TPSO (CMI) \quad CGD (PDF) \quad DIT (JSP) \quad HomePro \quad MegaHome \quad Boonthavorn \quad +5 แหล่ง};
    \node[lbl,right=0.2cm of L1.north west,yshift=-0.35cm]
      {ชั้นที่ 1: แหล่งข้อมูล (11 sources)};

    % Layer 2
    \node[layer,fill=blue!8,below=0.5cm of L1] (L2)
      {PDF Parser (unpdf) \quad JSON API (suggest.jsp) \quad ScrapingBee Proxy \quad AI Agent Research};
    \node[lbl,right=0.2cm of L2.north west,yshift=-0.35cm]
      {ชั้นที่ 2: การดึงข้อมูล (4 strategies)};

    % Layer 3
    \node[layer,fill=green!8,below=0.5cm of L2] (L3)
      {Data Cleaning \quad Unit Standardization \quad Canonicalization (3-tier) \quad Outlier Detection (z-score)};
    \node[lbl,right=0.2cm of L3.north west,yshift=-0.35cm]
      {ชั้นที่ 3: ประมวลผลข้อมูล};

    % Layer 4
    \node[layer,fill=red!8,below=0.5cm of L3] (L4)
      {Cloudflare KV — price snapshots \quad\quad history time-series \quad\quad fact\_calculations (BOQ log)};
    \node[lbl,right=0.2cm of L4.north west,yshift=-0.35cm]
      {ชั้นที่ 4: จัดเก็บข้อมูล (Cloudflare KV)};

    % Layer 5
    \node[layer,fill=violet!8,below=0.5cm of L4] (L5)
      {REST API (Next.js) \quad BOQ Calculator (5 work types) \quad Statistics (trend, region) \quad Web UI (QS user)};
    \node[lbl,right=0.2cm of L5.north west,yshift=-0.35cm]
      {ชั้นที่ 5: บริการแก่ผู้ใช้};

    % Arrows
    \draw[arr] (L1.south) -- (L2.north);
    \draw[arr] (L2.south) -- (L3.north);
    \draw[arr] (L3.south) -- (L4.north);
    \draw[arr] (L4.south) -- (L5.north);
  \end{tikzpicture}
  \caption{สถาปัตยกรรมระบบรวบรวมและคำนวณราคาวัสดุก่อสร้างแบบ 5 ชั้น (5-layer architecture). ชั้นล่างสุดให้บริการต่อวิศวกรประมาณราคา (Quantity Surveyor) ผู้เป็นกลุ่มผู้ใช้หลัก.}
  \label{fig:arch}
\end{figure}
